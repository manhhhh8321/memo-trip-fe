import { BackIcon } from "@assets";
import { Button } from "antd";

export const BottonShare = () => {
    return (
        <Button>
            <p>Login</p>
            <BackIcon />
        </Button>
    );
};
